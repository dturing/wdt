{
  "name": "wdt",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil App Starter",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "deploy": "rsync -ac wdt/ turing@danturing.com:wdt.danturing.com/"
  },
  "dependencies": {
    "@stencil/core": "^0.17.2",
    "@stencil/router": "^0.3.3",
    "lodash": "^4.17.15"
  },
  "license": "MIT",
  "devDependencies": {
    "workbox-build": "3.4.1"
  }
}
